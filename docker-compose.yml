version: "3.9"

services:
  fastapi-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app               # Mount source code for live reload
      - ./data:/app/data     # Persist SQLite DB
    environment:
      - PYTHONUNBUFFERED=1
